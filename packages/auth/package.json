{
  "name": "@cloud-commerce/auth",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@cloud-commerce/config": "workspace:*",
    "@cloud-commerce/domain": "workspace:*",
    "@cloud-commerce/logging": "workspace:*",
    "aws-jwt-verify": "^4.0.1"
  }
}
