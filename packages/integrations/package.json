{
  "name": "@cloud-commerce/integrations",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@cloud-commerce/domain": "workspace:*",
    "@cloud-commerce/logging": "workspace:*"
  }
}
