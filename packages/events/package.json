{
  "name": "@cloud-commerce/events",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean"
  },
  "dependencies": {
    "@aws-sdk/client-eventbridge": "^3.614.0",
    "@cloud-commerce/domain": "workspace:*",
    "@cloud-commerce/logging": "workspace:*"
  }
}
